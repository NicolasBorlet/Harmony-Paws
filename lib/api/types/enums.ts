export enum ActivityVisibility {
  PRIVATE = 'private',
  PUBLIC = 'public',
}

export enum DogSex {
  MALE = 'male',
  FEMALE = 'female',
}

export enum ActivityType {
  FOREST = 'forest',
  CITY = 'city',
  PLAGE = 'plage',
}

export enum DogDominance {
  NEUTRAL = 'neutral',
  DOMINANT = 'dominant',
  DOMINATED = 'dominated',
}
